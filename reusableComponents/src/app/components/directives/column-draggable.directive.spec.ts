import { ColumnDraggableDirective } from './column-draggable.directive';

describe('ColumnDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
